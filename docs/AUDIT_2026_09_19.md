# Fresh-clone release audit — 19 September 2026

Audited GitHub `main` at `7c0a1d41205d3d1e1adea1338c14e648a5e9f776`, then tested the corrections in this commit. The selected model and all five engine binaries remain unchanged.

## Identity

- Root checkpoint SHA-1: `40add39d927e5c265a7adbee22321932f7c48791`.
- Shared reconstruction head, 1,346,360 parameters, exits 3/6/10/13/16.
- Selected `best_frontier.pt` from the completed 120-epoch run; no combined-data fine-tune is included.
- Frozen bundle verifier: all 53 files passed, including the training checkpoint, deployment weights, ONNX files and five engines.

## Executed checks

- CPU: all 13 input stress cases passed.
- Local RTX 4050 CUDA: all 13 input stress cases passed with PyTorch fallback (TensorRT is not installed).
- All 297 reference outputs: CPU exact; CUDA FP32 maximum absolute difference `9.537e-07`, below the explicit `1e-5` threshold.
- Isolated packaging test: all 297 outputs finite float32, clamped to [0,1], correct filenames and exactly twice the input dimensions. A four-item package invoked from an unrelated directory produced identical results (`max_abs=0`).
- Eight shared-release tests passed: all five exits match the reference architecture exactly; legacy loading, adapter rejection, batch splitting, weight guards, fallback and strict rejection are covered. Mocked TensorRT tests establish dispatch behavior, not hardware execution.
- CPU environment doctor passed all required checks. Python compilation and PowerShell setup syntax passed.

## Corrections

- Doctor used a superseded checkpoint hash; updated to the selected shared model.
- Reference checker compared GPU FP16 against CPU FP32 byte-for-byte. It now requests true FP32, accepts a documented cross-device tolerance, uses a fresh output folder and checks every input filename.
- Added `--require-trt` to prevent silent fallback during hardware acceptance, including missing-helper, incompatible-runtime and unsupported-size cases.
- Prevented input/output folder aliasing from overwriting source images.
- Setup requirements now describe the current inference environment. Root requirements remain self-contained for the minimal package. Removed silent unpinned installation fallback.
- Archived old active gate-engine metadata and old release instructions; preserved historical research results with current-release pointers.

## Hardware limitation

The H100 pod was terminated. This audit did **not** execute the updated root TensorRT integration on an H100 or deserialize its engines on that GPU. Historical H100 build, numerical checks and 100-image evaluation records remain in the frozen deployment bundle. Local checks cannot replace that hardware test.

On the matching Linux H100 environment, install `requirements-h100.txt` and run `python tools/audit_h100.py INPUT_DIR AUDIT_OUTPUT_DIR`. The acceptance helper requires actual TensorRT for all five exits and tests real inputs plus 32/128/256/512 shape probes. It exits on any fallback or numerical failure.
