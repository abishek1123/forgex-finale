# Engine location

Current shared-head H100 engines are in `deployment/h100_shared/models_native_r1`.
The previous gate/TensorRT-11 index is preserved in `docs/history/previous_gate_engine_metadata` and is not an active engine set for these weights.
