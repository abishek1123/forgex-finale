# Current inference environment. Use requirements-h100.txt on the matching Linux H100 target.
# The previous training freeze is retained in docs/history/previous_requirements.txt.
torch==2.8.0
numpy>=2.1,<3
